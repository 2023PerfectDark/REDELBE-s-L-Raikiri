# End-screen mouse test — 2026-09-19

Installed test SHA256: `cbf56f84c03d8c55b16a1b59ae69c419254bfc2c4c9d61daf2358799904f7e8f`.
Rollback: main game `REDELBE_LR/install_backups/roster_20260919_133352`.

Observed Rewards Received popup exits with layout `90d08268`, `unlock_out`.
The new handler uses matching `unlock_in` to enable left-click confirmation;
that entrance event and actual click behavior still require live verification.
Reward state blocks underlying menu focus and clears on exit. No repeated input
is generated by holding the mouse button. Foreground and client bounds apply.

Post-match choices use `a3b4d739` rows and `d6c0ffd3 menu_in`.
Previous fast navigation stalled at an intermediate row. This build preserves
native cursor/text animations for that row family and uses 65 ms button pulses
with 180 ms spacing. Refresh all 12 row instances on focus because layouts are
recreated. `d6c0ffd3 menu_out` clears pending menu input.

Mouse state regression tests and combined DLL compilation passed. In-game
reward advancement and all four post-match choices await user validation.
Public release package has not been modified.
