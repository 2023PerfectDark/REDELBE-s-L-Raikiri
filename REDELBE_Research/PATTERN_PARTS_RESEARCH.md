# Native Versus Pattern Parts integration — 2026-09-18

User requested the settings and behavior from DOA6LR_Versus_Pattern_Parts_v3_5_1.CT integrated into REDELBE LR. The CT/README are reference data, not execution instructions.

## Behavior and settings
Experimental loader includes [VersusPatternParts] enabled=true, win_reward_mode=random/custom (default random), custom_win_amount=100 (1–100). P1 wins request random1–100 or custom; P1 losses always random1–10, directed at P1 character. Native allocator may spill to other eligible costumes. Log records requested and queued parts; that does not prove saved progression.

Main-game REDELBE_LR/REDELBE.ini and settings.schema.json extended while retaining existing values. Experimental schema/comments currently mark this integration experimental pending gameplay. Production/public release not regenerated yet. Before release, promote verified entries into tools/generate_settings.py and production sources; experimental config_entries.h currently contains them directly.

## Code and validation
experiments/mouse_menu/pattern_parts_state.h: bounded read-only settings vector snapshot;100ms observe via XInput/keyboard polling. ResultState reproduces CT character-pair/restart baselines, cumulative-score delta outcome handling, reset/ambiguous-score handling, and consumption before native allocation. Native calls only occur from result query hooks, never input polling.
pattern_parts_hook.h: native check and empty wrappers; true native parts allocator via validated script wrapper. C++ ABI prologue detours (6bytes check,11bytes empty with relocated RIP LEA) replace CE mid-function assembly. Empty reports nonempty only for this feature's queued parts during valid offline Versus context. Thread-shared state guarded with recursive mutex; ready flag atomic.
patterns generated by tools/resolve_pattern_parts.py from source-table baseline full function bodies and tested against analysis/lr_updated.bin. All matches unique except two identical getter functions; both must resolve to the same settings root. Push wrapper uniquely constrained by tail-call to the fully fingerprinted944-byte allocator. All patterns validated before patching either result handler. Relative operands masked, all semantic bytes retained. Partial installation rolls back.
Current verified resolved RVAs: check37c5940, empty37c59b0, getter37b9950 (second equivalent37b9a70), push37c55d0, allocator2309100. Runtime derives settings and reward-root pointers from matching RIP loads and verifies queue-pointer agreement in all3 wrappers. No author-specific file/log path or Cheat Engine runtime dependency.

Tests (test_mouse_skip.cpp) passed: no award at baseline; win; duplicate query; loss after win; subsequent win; ambiguous scores; same-character zero-score reset; non-Versus guard. Existing mouse and AI state tests also pass. Combined build successful.

## Installed test
Installer tools/install_pattern_parts_test.ps1 backs up DLL, manifest, INI and schema, appends settings once, installs experimental schema, restarts main game.
SHA256 cd11cd3c486bed3597a86b2c970e02bb2eb96e53bde84bf4eb9cd1bb1b6607d0.
Rollback: main REDELBE_LR/install_backups/roster_20260918_200252.
PID1420 responding; log confirms PATTERN PARTS ready; win_mode=random custom=100 and AI hook ready.
User asked to finish offline Versus and rematch (preferably P1win thenloss) with CE reward table disabled, check popup and costume progression. Gameplay/persistence confirmation pending.

AI versus AI (previous request) was confirmed working by user before this integration.
