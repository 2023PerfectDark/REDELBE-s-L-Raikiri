To install, copy the REDELBE folder inside "Story mode stages" and "Random costume" and merge it with the one of the game.

To use:
- Random character: just select like any other costume layer2 mod (all of them are selected in default char slot)
- Story mode stages: just select like any other stage layer2 mod.